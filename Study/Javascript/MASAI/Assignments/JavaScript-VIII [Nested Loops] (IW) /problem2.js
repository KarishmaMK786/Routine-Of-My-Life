// Problem 2: Print Prime Numbers from 1 to given limit

let start = 1;
let limit = 30;

// prime numbers 

 for(start; start<=limit; start++) {
    let count = 0;
    if(i%2 == 0) {
        count++;
    }
    if(count>=1) {
        console.log(start);
    }
 }
